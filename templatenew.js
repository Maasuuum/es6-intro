const first = 'Fardous'
const middle = 'Al'
const last = 'Masum'

const name = first + ' ' + middle + ' ' + last;

console.log(name);

const a = 10;
const b = 20;
const result = 'The sum of ' + a + ' and ' + b + ' is ' + (a + b);
console.log(result);
const math = `The sum of ${a} and ${b} is ${a+b}`
console.log(math);

// Multiple line string
const email = `Hello there! \n` +
`How are you? \n` +
`How old are you?`
;
console.log(email);

const email2 = `Hello there!
How are you?
How old are you?`;
console.log(email2);
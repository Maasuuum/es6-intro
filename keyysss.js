const cigerattee = { 
    name: 'benson', 
    color: 'golden', 
    price: 12, 
    isCleaned: true
};

console.log(cigerattee);
const keys = Object.keys(cigerattee);

const values = Object.values(cigerattee);
// console.log(values);

const pair = Object.entries(cigerattee);
console.log(pair);